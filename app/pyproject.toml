[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic",
    "requests",
    "beautifulsoup4",
    "sqlalchemy>=2.0.49",
    "fastapi>=0.136.0",
]
