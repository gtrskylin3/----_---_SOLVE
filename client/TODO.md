# План работ по клиентской части (Frontend)

## Этап 1: Настройка проекта
- [x] Создать Vue.js проект с помощью Vite.
- [x] Установить и настроить Tailwind CSS для стилизации.
- [x] Создать базовую структуру директорий (`components`, `views`, `router`, `services`).
- [x] Установить и настроить Vue Router для навигации между страницами.
- [x] Подключить библиотеку MathJax в `index.html` для корректного отображения математических формул.

## Этап 2: Создание интерфейса и навигации
- [x] Реализовать компонент `Header.vue` с навигационными ссылками "Задачи" и "Статистика".
- [x] Настроить Vue Router для переключения между страницами `TasksView` и `DashboardView`.
- [x] Скопировать и адаптировать базовые стили из примера для создания темной темы (`style.css`).

## Этап 3: Реализация страницы "Задачи" (`TasksView.vue`)
- [x] Создать сервис `api.js` для инкапсуляции запросов к бэкенду (получение задач, проверка ответа).
- [x] В `TasksView.vue` реализовать функцию для загрузки списка задач с сервера при монтировании компонента.
- [x] Создать компонент `TaskCard.vue`, который принимает объект задачи как `prop`.
- [x] В `TaskCard.vue` отобразить текст задачи, используя `question_html` и директиву `v-html`.
- [x] Добавить в `TaskCard.vue` поле для ввода ответа и кнопку "Проверить".
- [x] Реализовать логику отправки ответа на сервер при нажатии кнопки и отображение результата ("Верно" / "Неверно").
- [x] Добавить базовые состояния загрузки и ошибок.

## Этап 4: Создание заглушки для "Статистики" (`DashboardView.vue`)
- [ ] Создать компонент `DashboardView.vue` с заголовком "Статистика" и текстом-заглушкой "Этот раздел будет реализован в будущем".
